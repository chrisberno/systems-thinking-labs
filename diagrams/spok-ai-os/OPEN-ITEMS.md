# SPOK OS — OPEN ITEMS (front & center)

> **Glance here first, every session on this work.** Living list — keep it current.
> Last updated: 2026-06-11.

## ✅ Done (this thread)
- **Ring 1 / core LOCKED:** `AGENT = LLM (token-metered) + Soul (owned)` → **DETERMINES YOUR OUTCOMES ★**. Spec: `core-agent-definition.md`. Committed.
- Interactive **editor** (`editor.html`/`editor.js`) + **checkpoint tool** (`snapshot.js`) — live, committed, snapshots accumulating in `~/Desktop/spok-os-diagram/`.
- Diagram retheme: teal-on-dark palette; **Connectors-outermost** order (locked membrane doctrine).
- **Soul traits rescued** from `~/.claude` memory → `~/SPOK/soul/traits/` (git germline). Body-coupling fixed (minimal pass).

## 🔧 Open — soul germline (Ring-1 build; gate consciously opened for this)
- [ ] **Author `soul.md`** — the 5 invariants (Allegiance · Values · Taste · Stance · Refusals). Needs the **Values forge** (virtue set is still a stub).
- [ ] **Re-sort rescued traits** — `feedback_ssh_key_sharing` + `feedback_creds_propagate_tailscale` are **ops, not soul** → move to the Capabilities/ops layer when `soul.md` lands.
- [ ] **Canonical↔projection sync** — `~/SPOK/soul/` is canonical; `~/.claude` memory is the live harness copy. Wire sync/symlink (or fold into `soul.md`) to kill the **drift risk**. *(Until then: edit canonical, mirror by hand.)*

## 🎨 Open — diagram naming pass (inside-out; only Ring 1 done)
- [ ] **Ring 2: CONTEXT → BRAIN** (deepspok hippocampus + vaults cortex) + tab rework.
- [ ] **Ring 3: CAPABILITIES** — revisit name + tab.
- [ ] **Ring 4: CONNECTORS** — revisit name + tab.

## 🧹 Open — artifacts / cleanup
- [ ] Strip the dropped *"own the soul, rent the body"* + *"body"* language from the Desktop explainer doc + old infographic prompt.
- [ ] **When vision locks:** back-port the editor MODEL → canonical `config.json`, regenerate, *then* graduate per gate.
- [ ] NotebookLM audio/PNG redo (parked — current drafts in `drafts/` on the MINI).

## 🚧 GATE
The onion is **NOT locked** — we're mid naming pass (Ring 1 only). **No graduation** to `~/SPOK/os` or deepspok capture until the CEO is stoked. *(The soul-traits rescue is a deliberate, CEO-authorized exception.)*
